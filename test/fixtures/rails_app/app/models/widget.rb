# frozen_string_literal: true

# This standalone fixture intentionally has no ApplicationRecord.
# standard:disable Rails/ApplicationRecord
class Widget < ActiveRecord::Base
end
# standard:enable Rails/ApplicationRecord
